{
  "version": 2,
  "builds": [
    {
      "src": "ENADE.py",
      "use": "@vercel/python",
      "config": { "maxDuration": 15 }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "ENADE.py"
    }
  ]
}
